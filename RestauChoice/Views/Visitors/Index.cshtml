@model IEnumerable<RestauChoice.Models.Visitor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Login)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Mdp)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Login)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Mdp)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VisitorId }) |
            @Html.ActionLink("Details", "Details", new { id=item.VisitorId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VisitorId })
        </td>
    </tr>
}

</table>
